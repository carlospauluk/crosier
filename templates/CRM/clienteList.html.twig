{% extends 'list.html.twig' %}


{% set titulo = 'Clientes' %}
{% set route_form = 'crm_cliente_form' %}
{% set route_list = 'crm_cliente_list' %}
{% set route_list_ajax = 'crm_cliente_datatablesJsList' %}
{% set list_id = 'clienteList' %}

{% set campos_pesquisar %}

    <div class="form-group">
        <label for="p_descricao">Nome</label>
        <input type="text" class="form-control" id="filter[p_nome]"
               name="filter[p_nome]"
               value="{{ filter.p_nome|default('') }}">
    </div>
    <div class="form-group">
        <label for="p_descricao">Código</label>
        <input type="number" size="15" class="form-control"
               name="filter[codigo]"
               value="{{ filter.codigo|default('') }}">
    </div>
{% endset %}


{% block down_scripts %}
    <script src="{{ asset('build/crm/clienteList.js') }}"></script>
{% endblock %}